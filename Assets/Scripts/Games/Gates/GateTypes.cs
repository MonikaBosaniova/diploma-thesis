namespace Gates
{
    /// <summary>
    /// Enumeration of supported logic gate types in the gates minigame
    /// </summary>
    public enum GateTypes
    {
        None,
        NOT,
        AND,
        OR,
        XOR
    }
}
